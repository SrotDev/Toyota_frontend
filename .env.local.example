# Example environment overrides for frontend
# Uncomment and set to call a remote/staging backend instead of local proxy
# VITE_API_BASE=https://staging.example.com/api
# By default the app uses '/api' which is proxied via vite.config.ts to http://127.0.0.1:8000
